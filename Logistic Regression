
## Supervised Learning Training Loop

X → model(θ) → Ŷ → compare with Y → cost → update θ → repeat
Turn text into numbers → guess → compare → fix mistakes → finally classify sentiment.

Build a vocabulary of all unique words
Convert each tweet into a sparse 0/1 vector showing which words appear
but a large vocabulary makes vectors long and slows training and prediction
---
